import Contact from "../components/Contact"

function ContactSection({id}) {

    return (
        <section id={id}>
            <Contact />
        </section>
    );
};

export default ContactSection;