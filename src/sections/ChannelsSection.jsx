import Channels from "../components/Channels";

function ChannelsSection({ id }) {

    return (
        <section id={id}>
            <Channels />
        </section>
    );
};

export default ChannelsSection;